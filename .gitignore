# Node.js dependencies (applies to both client and server node_modules)
node_modules/

# Environment variables
.env
.env.local
.env.development.local
.env.test.local
.env.production.local

# Build artifacts
build/
dist/
public/build
coverage/

# Logs and databases
*.log
npm-debug.log*
.sqlite
.sqlite-journal
*.db

# OS and IDE specific files
.DS_Store
Thumbs.db
.vscode/
.idea/

# Client-side specific (if not handled by global patterns)
# If your create-react-app is in a 'client' folder, this handles potential client-specific build folders
client/build
client/coverage

# Server-side specific (if needed)
# Add specific ignore patterns for MongoDB files if they are stored locally and shouldn't be committed
dump/
*.mongo
